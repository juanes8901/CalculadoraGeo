//se creo como java interface y no como java class
package JECM_Controlador;
//siguiendo no hay provate (atributos)
public interface JECM_Figura {
    public double obtenerarea();
    public double obtenerVolumen();
}
